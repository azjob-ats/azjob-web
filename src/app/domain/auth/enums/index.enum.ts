export enum eIndex {
  INDEX = 'index',
}

export enum eProvider {
  GOOGLE = 'google.com',
  AZJOB = 'azjob.com',
}

export enum eRole {
  ADMIN = 'admin',
  CHIEFEDITOR = 'chiefeditor',
  EDITOR = 'editor',
  USER = 'user',
}
